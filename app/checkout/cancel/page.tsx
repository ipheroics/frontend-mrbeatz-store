export default function CancelPage() {
  return (
    <div className="card">
      <h1 className="text-2xl font-semibold">Payment Canceled</h1>
      <p className="text-[color:var(--muted)] mt-2">Your cart is still saved if you want to try again.</p>
    </div>
  );
}
